# 获取用户专属参会链接

可以获取指定会议的所有专属参会链接及 `customer_data` 。

> 说明
> - 该接口不支持个人会议号会议、网络研讨会（Webinar）。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/meeting/get\_customer\_short\_url?access\_token=ACCESS\_TOKEN

**请求包体：**

```json
{
	"meetingid": "meetingid11234"
}
```

**参数说明：**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| access\_token | 是 | string | 调用接口凭证。获取方法查看“[获取access\_token](./15074.md)” |
| meetingid | 是 | string | 会议ID |

**权限说明**

* 仅配置在“可调用接口的应用”列表中的自建应用可调用
* 仅允许获取该应用创建的会议的数据

**返回结果：**

```javascript
{
	"errcode": 0,
	"errmsg": "ok",
	"meeting_short_url_customer_data_list": [{
		"customer_data": "CUSTOMERDATA",
		"meeting_short_url": "URL"
	}]
}
```

**参数说明**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| errcode | int32 | 返回码 |
| errmsg | string | 对返回码的文本描述内容 |
| meeting\_short\_url\_customer\_data\_list | object[] | 用户专属参会链接对象列表。详见CustomerData。 |

**CustomerData说明**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| customer\_data | string | 用户专属字段 |
| meeting\_short\_url | string | 用户专属参会链接 |
