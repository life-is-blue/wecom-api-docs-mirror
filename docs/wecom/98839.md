# 删除会议投票

删除会议投票。

> 仅进行中的会议可以调用该接口
> 操作者是主持人或者会议管理员

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/meeting/poll/delete?access\_token=ACCESS\_TOKEN

**请求包体**

```javascript
{
	"operator_userid":"OPENID",
	"instance_id":1,
	"meetingid":"MEETINGID",
	"poll_theme_id":"theme",
	"poll_id":"ID"
}
```

**参数说明：**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| access\_token | 是 | string | 调用接口凭证。获取方法查看“[获取access\_token](./15074.md)” |
| operator\_userid | 是 | string | 操作者的openid |
| instance\_id | 是 | int | 操作者入会的设备id |
| meetingid | 是 | string | 会议ID |
| poll\_theme\_id | 否 | string | 投票主题 ID，传入则代表删除投票主题，删除投票主题不影响投票实例。 投票主题 ID 和投票 ID 二选一，如果都传入，会使用投票 ID。 |
| poll\_id | 否 | string | 投票 ID，传入则代表删除投票实例。当主题下所有主题实例被删，则投票主题也被删除。 投票主题 ID 和投票 ID 二选一，如果都传入，会使用投票 ID。 |

**返回结果**

```javascript
{
	"errcode": 0,
	"errmsg": "ok"
}
```

**参数说明：**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| errcode | int32 | 返回码 |
| errmsg | string | 对返回码的文本描述内容 |
