# 获取等候室成员记录

获取等候室成员列表，包括等候室内所有进出过的成员。会前、会中、会后都可以获取。

**请求方式：**POST（**HTTPS**）
**请求地址：**https://qyapi.weixin.qq.com/cgi-bin/meeting/waitingroom/get_user_list?access_token=ACCESS_TOKEN

**请求包体：**

```javascript
{
	"meetingid": "MEETINGID",
	"limit": 20,
	"cursor": "XMVEI"
}
```

**参数说明：**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| access_token | 是 | string | 调用接口凭证。获取方法查看“[获取access_token](#15074)” |
| meetingid | 是 | string | 会议ID |
| limit | 否 | int32 | 分页大小，默认20，最大50 |
| cursor | 否 | string | 分页查询用，将上一个请求返回的next_cursor字段传入。第一次查询时可不传值 |

> 注意：
> limit参数必须与首次调用获得cursor时传入的limit一致。

**权限说明**

* 仅配置在“可调用接口的应用”列表中的自建应用可调用
* 仅允许获取该应用创建的会议的数据
* 仅返回在应用可见范围内的成员

**返回结果：**

```javascript
{
	"errcode": 0,
	"errmsg": "ok",
	"has_more":true,
	"next_cursor":"NGIENIG",
	"user_list": [{
		"userid": "USERID",
		"tmp_openid": "tmpid",
		"instance_id": 1,
		"join_time": 13525325,
		"quit_time": 12491059320
	}]
}
```

**参数说明**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| errcode | int32 | 返回码 |
| errmsg | string | 对返回码的文本描述内容 |
| has_more | bool | 是否还有更多成员列表 |
| next_cursor | string | 当has_more=true时有值，下次请求将该字段赋值给cursor字段 |
| userid | string | 企业成员userid |
| tmp_openid | string | 会议中为每个参会成员授予的临时 ID |
| instance_id | int32 | 成员的终端设备类型： 0：PSTN 1：PC 2：Mac 3：Android 4：iOS 5：Web 6：iPad 7：Android Pad 8：小程序 9：voip、sip 设备 10：linux 20：Rooms for Touch Windows 21：Rooms for Touch MacOS 22：Rooms for Touch Android 30：Controller for Touch Windows 32：Controller for Touch Android 33：Controller for Touch iOS 81：鸿蒙手机 82：鸿蒙平板 83：鸿蒙PC 84：鸿蒙汽车 |
| join_time | int64 | 加入时间（单位：毫秒） |
| quit_time | int64 | 离开时间（单位：毫秒） |
