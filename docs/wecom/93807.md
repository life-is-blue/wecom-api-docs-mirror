# 进入会议

## ww.startMeeting(params)

> 兼容性：企业微信 3.1.0 开始支持

### 功能描述

进入会议

通过传入 meetingId 进入已有会议。

```ts
ww.startMeeting({
  meetingId: 'MEETINGID'
})
```

### 使用限制

* 本接口必须使用应用身份进行注册
* 应用必须具有会议使用权限，参考[配置可使用会议的应用](./93626.md#%E9%85%8D%E7%BD%AE%E5%8F%AF%E4%BD%BF%E7%94%A8%E4%BC%9A%E8%AE%AE%E7%9A%84%E5%BA%94%E7%94%A8)
* 用户需要在应用可见范围内

### 参数说明

#### params: Object

|  | 属性 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
|  | meetingId | string | 否 | 会议 ID，可通过[创建预约会议](./99104.md)或通过调用ww.startMeeting()获取 为空的情况下创建快速会议 |
|  | success | Function | 否 | 成功回调 |
|  | fail | Function | 否 | 失败回调 |
|  | cancel | Function | 否 | 取消回调 |
|  | complete | Function | 否 | 完成回调 |

### 返回说明

**Promise<Object>**

|  | 属性 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
|  | meetingId | string | 是 | 会议 ID |

### 错误信息

| errMsg | 说明 |
| --- | --- |
| startMeeting:ok | 执行成功 |
| startMeeting:fail no permission | 应用签名校验失败，或成员不在应用的可见范围内，或应用未开启会议使用权限 |
| startMeeting:fail user not in allow list | 会议发起人必须在应用可见范围 |
| startMeeting:fail not allow to cross corp | 不可跨企业使用会议ID |
| startMeeting:fail invalid meeting id | 不合法的会议ID |
| startMeeting:fail not allow to cross app | 不可跨应用使用会议ID |
| startMeeting:fail invalid parameter | 参数不合法 |

## 旧版jweixin调用

### 示例代码

**调用示例**

```javascript
wx.invoke('startMeeting', {
		"meetingId": "MEETINGID",
	}, function(res) {
		if (res.err_msg == "startMeeting:ok") {
        }
    });
```
