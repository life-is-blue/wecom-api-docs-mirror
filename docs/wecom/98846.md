# 修改会议基础布局

根据布局 ID 对设置好的会议基础布局进行修改。
用户座次设置区分会前和会中两种方式：会前只允许设置邀请者成员，会中只允许设置参会成员。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/meeting/layout/update?access\_token=ACCESS\_TOKEN

**请求包体：**

```javascript
{
	"meetingid": "MEETINGID",
	"layout_id": "LAYOUTID1",
	"page_list":[{
		"layout_template_id": "TEMID1",
		"user_seat_list":[{
			"grid_id":"10",
			"grid_type":1,
			"userid": "USERID",
			"tmp_openid": "68j2xkYT1",
			"nick_name": "NICK",
			"tool_sdkid": "SDKID"
		}]
	}],
	"enable_set_default": false
}
```

**参数说明：**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| access\_token | 是 | string | 调用接口凭证。获取方法查看“[获取access\_token](https://work.weixin.qq.com/api/doc/90001/90143/90605)” |
| meetingid | 是 | string | 会议 ID |
| layout\_id | 是 | string | 布局 ID |
| page\_list | 是 | object[] | 布局对象列表，详见LayoutPage |
| enable\_set\_default | 否 | bool | 是否设置为会议应用的布局，默认不设置 |

**LayoutPage 说明**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| layout\_template\_id | 是 | string | 布局模板 ID |
| user\_seat\_list | 否 | object[] | 用户座次对象列表，详见UserSeat |

**UserSeat 说明**

| 参数 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| grid\_id | 是 | string | 宫格 ID。若多次传入同一宫格ID的对象，仅第一次出现的对象生效。 |
| grid\_type | 是 | uint32 | 宫格类型： 1. 视频画面 2. 共享画面 3. 拓展应用（目前一页仅可添加一个应用）。添加的应用需满足以下条件：1) 与会议绑定。2) 开启网页服务。3) 同企业下的仅企业内可见应用或外部企业可见应用。 |
| userid | 否 | string | 当场会议的企业成员的userid |
| tmp\_openid | 否 | string | 当场会议的用户临时 ID |
| nick\_name | 否 | string | 当宫格类型(grid\_type)为1时必填，作为视频画面展示 |
| tool\_sdkid | 否 | string | 拓展应用 ID |

**返回结果：**

```javascript
{
	"errcode": 0,
	"errmsg": "ok"
}
```

**参数说明：**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| errcode | int32 | 返回码 |
| errmsg | string | 对返回码的文本描述内容 |
